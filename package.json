{
  "name": "scrow",
  "private": true,
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix sdk install",
    "dev": "npm --prefix server run dev",
    "dev:open": "powershell -NoProfile -Command \"Start-Process http://localhost:4000; npm --prefix server run dev\"",
    "build": "npm --prefix server run build && npm --prefix sdk run build"
  }
}

